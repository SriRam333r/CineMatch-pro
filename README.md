# CineMatch-pro
CineMatch Pro is a stateful web application built entirely in Python using the Streamlit framework. The backend data pipeline utilizes the Pandas library to ingest, clean, and merge a 200,000-row subset of the MovieLens dataset.
